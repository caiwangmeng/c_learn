Summary:
1. 变量和函数，先声明再引用     -- 声明的目的让栈预留空间，也可以声明并引用
2. 指针也是变量





Attention:
0. 一定要初始化变量，这在所有编程语言实践中都是好习惯
1. sizeof 返回的是对象的大小，字节为单位，不是对象中元素的数目，尤其注意数组
2. 宏定义函数参数，必须要有括号!!!
3. 数组做为参数的时候，退化为指针!!!(数组本身可以引用，但是sizeof不可以)
4. C 不支持在函数外返回局部变量的地址，除非定义局部变量为 static 变量
    -- 基本数据类型指针复用



Question:
1. int为什么可能是2字节，也可能是4字节
-- 编译器决定，标准不同，还有X32和X64系统的区别

2. 打印地址为什么不是16进制的表示，即0X开头    main_pointer.c
-- Linux系统是0x开头，windows不是

3. 函数重名问题，不同作用域和同作用域
-- 不能重名
// 对比Java, c++ 等面向对象重名

4. 宏的定义是全局有效，还是当前文件有效?

5. 关于菜鸟教程内存管理，示例1中description指针内存大小的问题?为什么是8字节
-- 指针在64位系统占用8字节，指针占用内存大小和操作系统有关
-- ??? 字符串占用内存大小

6. float 最大最小值?


Learn:
1. C语言指针详解，见末尾      https://www.runoob.com/cprogramming/c-pointers.html
3. 指针指向一个变量或者函数之后，变量/函数更改地址，指针指向地址是否跟着改变
4. 头文件:
#include <stdlib.h>
#include <stdio.h>
5. size_t